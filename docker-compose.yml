services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: band
      POSTGRES_USER: band
      POSTGRES_PASSWORD: band
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U band -d band"]
      interval: 5s
      timeout: 5s
      retries: 10

  localstack:
    image: localstack/localstack:4
    restart: unless-stopped
    ports:
      - "4566:4566"
    environment:
      SERVICES: sqs
      AWS_DEFAULT_REGION: ap-northeast-2
      DEBUG: 0
    volumes:
      - ./docker/localstack/init-aws.sh:/etc/localstack/init/ready.d/init-aws.sh
      - localstack-data:/var/lib/localstack
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:4566/_localstack/init/ready | grep -q '\"completed\": true'"]
      interval: 5s
      timeout: 5s
      retries: 20

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    # @bandapp/types는 dist를 배포하는 패키지라 dev 기동 전에 빌드가 필요하다.
    # db:migrate(drizzle migrate)는 멱등이라 매 기동마다 실행해도 안전하다.
    command: sh -c "pnpm --filter @bandapp/types build && pnpm --filter @bandapp/api db:migrate && exec pnpm --filter @bandapp/api start:api:dev"
    env_file: [.env]
    environment:
      DATABASE_URL: postgresql://band:band@postgres:5432/band
      SQS_ENDPOINT: http://localstack:4566
      TSC_WATCHFILE: DynamicPriorityPolling
    ports:
      - "${API_PORT:-3000}:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/api/node_modules
      - /app/apps/mobile/node_modules
      - /app/packages/types/node_modules
      - /app/packages/config/node_modules
      - /app/packages/api-client/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      localstack:
        condition: service_healthy

  worker:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    command: sh -c "pnpm --filter @bandapp/types build && exec pnpm --filter @bandapp/api start:worker:dev"
    env_file: [.env]
    environment:
      DATABASE_URL: postgresql://band:band@postgres:5432/band
      SQS_ENDPOINT: http://localstack:4566
      TSC_WATCHFILE: DynamicPriorityPolling
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/api/node_modules
      - /app/apps/mobile/node_modules
      - /app/packages/types/node_modules
      - /app/packages/config/node_modules
      - /app/packages/api-client/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      localstack:
        condition: service_healthy

  audio-worker:
    build:
      context: .
      dockerfile: apps/audio-worker/Dockerfile
    restart: unless-stopped
    env_file: [.env]
    environment:
      SQS_ENDPOINT: http://localstack:4566
    volumes:
      - ./apps/audio-worker/src:/app/src
    depends_on:
      localstack:
        condition: service_started

volumes:
  postgres-data:
  localstack-data:
